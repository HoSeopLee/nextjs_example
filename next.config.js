/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  env: {
    SANITY_PROJECT_ID: 't5ey18mj',
  },
  // trailingSlash:true, //뒤에 슬래쉬안쓰는거 지울지 말지
};

module.exports = nextConfig;
